"use strict";

const {KJUR} = require("jsrsasign");

class Digest {
  
  static sha1(value) {
    let hash = new KJUR.crypto.MessageDigest({"alg": "sha1"});
    hash.updateString(value);
    return hash.digest();
  }

}

module.exports = Digest;
