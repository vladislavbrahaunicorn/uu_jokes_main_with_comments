import UU5 from "uu5g04";<%= context.libRootNamespaceLibName && context.libRootNamespaceLibName !== "uu5g04" ? `
import ${context.libRootNamespace} from "${context.libRootNamespaceLibName}";`
: `` %>
import <%= context.libRootNamespaceLibName ? "" : context.tagName.split(/\./)[0] + " from " %>"<%= context.name %>";

const { shallow } = UU5.Test.Tools;

describe(`<%= context.tagName %>`, () => {
  it(`default props`, () => {
    const wrapper = shallow(<<%= context.tagName %> />);
    expect(wrapper).toMatchSnapshot();
  });
});
