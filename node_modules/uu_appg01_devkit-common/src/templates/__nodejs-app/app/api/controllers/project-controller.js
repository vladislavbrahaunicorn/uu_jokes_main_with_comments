"use strict";
const <%= context.projectClassName%>Abl = require("../../abl/<%= context.projectClassFile%>-abl.js");

class <%= context.projectClassName %>Controller {
  init(ucEnv) {
    return <%= context.projectClassName%>Abl.init(ucEnv.getUri(), ucEnv.getDtoIn(), ucEnv.getSession());
  }
}

module.exports = new <%= context.projectClassName %>Controller();
