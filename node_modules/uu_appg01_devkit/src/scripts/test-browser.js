module.exports = class TestBrowser {
  constructor(config) {
    this.config = config;
  }
  async process() {
    this.config.set("jestRuntimeEnvironment", "browser");
    const Task = require("./test");
    await new Task(this.config).process();
  }
};
