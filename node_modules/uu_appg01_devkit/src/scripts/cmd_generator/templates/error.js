"use strict";

const <%= context.mainErrorClass%> = require("./<%= context.mainErrorFile%>");
const <%= context.constantName%>_ERROR_PREFIX = `${<%= context.mainErrorClass%>.ERROR_PREFIX}<%= context.entity%>/`;

<%= context.error%>;

module.exports = {
  <%= context.errorName%>
};
