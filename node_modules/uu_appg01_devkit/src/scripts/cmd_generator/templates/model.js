"use strict";
const Path = require("path");
const { Validator } = require("uu_appg01_server").Validation;
const { DaoFactory } = require("uu_appg01_server").ObjectStore;
const { ValidationHelper } = require("uu_appg01_server").AppServer;
const Errors = require("../api/errors/<%= context.classFile%>-error.js");

const WARNINGS = {
<%= context.cmdWarning%>
};

class <%= context.className%>Abl {

  constructor() {
    this.validator = Validator.load();
    <%= context.daoModifier%>this.dao = DaoFactory.getDao("<%= context.entity%>");
  }

<%= context.cmdMethod%>
}

module.exports = new <%= context.className%>Abl();
