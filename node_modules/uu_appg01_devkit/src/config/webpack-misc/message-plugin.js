class MessagePlugin {
  constructor(options) {
    this.options = options;
  }

  apply(compiler) {
    let { message } = this.options;
    compiler.hooks.done.tapAsync("MessagePlugin", (stats, callback) => {
      console.log(message);
      callback();
    });
  }
}

module.exports = MessagePlugin;
