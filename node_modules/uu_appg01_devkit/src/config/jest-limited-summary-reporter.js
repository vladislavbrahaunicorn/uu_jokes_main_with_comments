const SummaryReporter = require("@jest/reporters/build/summary_reporter.js").default;

module.exports = class LimitedSummaryReporter extends SummaryReporter {
  _printSummary(/*aggregatedResults, globalConfig*/) {
    // overridden without printing snapshot failure messages again
  }
};
