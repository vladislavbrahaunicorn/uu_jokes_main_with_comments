// Initialize testing configuration profiles
let testProfiles = "test,development,production";
if (!process.env["CONFIG_PROFILE"] && !process.env["NODE_ENV"]) {
  process.env["CONFIG_PROFILE"] = testProfiles;
  process.env["NODE_ENV"] = testProfiles;
}
