const path = require("path");

let buildDir = "target";

module.exports = {
  srcDir: "src",
  buildDir,
  buildWebpackTempDir: path.join(buildDir, "webpack-tmp"),
  buildLibWebDir: path.join(buildDir, "dist"),
  buildLibNodeDir: path.join(buildDir, "dist-node"),
  buildLibRootDir: path.join(buildDir, "dist-root"),
  buildLibEsmDir: path.join(buildDir, "dist-esm"),
  npmPackTranspiledNodeLibDir: "dist-node",
  npmPackTranspiledWebLibDir: "dist",
  npmPackEsmDir: "dist-esm",
  cdnPackDir: path.join(buildDir, "cdn-build"),
  testResultsProcessorOutputPath: path.join(buildDir, "test-report", "index.html")
};
