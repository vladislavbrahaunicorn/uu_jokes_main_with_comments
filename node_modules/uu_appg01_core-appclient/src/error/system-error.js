"use strict";

const RemoteError = require("./remote-error.js");

class SystemError extends RemoteError {
  constructor(message = null, response = null) {
    super(message, response);
    if (!this.status) {
      this.status = 500;
    }
  }
}

module.exports = SystemError;
