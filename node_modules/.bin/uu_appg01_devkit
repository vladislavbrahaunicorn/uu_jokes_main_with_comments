#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../uu_appg01_devkit/bin/uu_appg01_devkit.js" "$@"
  ret=$?
else 
  node  "$basedir/../uu_appg01_devkit/bin/uu_appg01_devkit.js" "$@"
  ret=$?
fi
exit $ret
